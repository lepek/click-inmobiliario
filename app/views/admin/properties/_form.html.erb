<%= form_for [:admin, @property] do |f| %>
<% if @property.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@property.errors.count, "error") %> prohibited this property from being saved:</h2>
	<ul>
		<% @property.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<fieldset class="gray">
	<div class="clearfix">
		<%= f.label :description, 'Descripción' %>
		<div class="input">
			<%= f.text_area :description %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :code, 'Código' %>
		<div class="input">
			<%= f.text_field :code %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :price, 'Precio' %>
		<div class="input">
			<%= f.number_field :price %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :currency_id, 'Moneda' %>
		<div class="input">
			<%= f.collection_select(:currency_id, Currency.order("name").all, :id, :name) %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :address, 'Dirección' %>
		<div class="input">
			<%= f.text_field :address %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :location_id, 'Localidad' %>
		<div class="input">
			<%= f.collection_select(:location_id, Location.order("name").all, :id, :name) %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :type_id, 'Tipo de propiedad' %>
		<div class="input">
			<%= f.collection_select(:type_id, Type.order("name").all, :id, :name) %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :operation_id, 'Tipo de operación' %>
		<div class="input">
			<%= f.collection_select(:operation_id, Operation.order("name").all, :id, :name) %>
		</div>
	</div>
	<div class="actions">
		<%= f.submit 'Guardar' %>
	</div>
</fieldset>
<% end %> 