<%= form_for [:admin, @type] do |f| %>
<% if @type.errors.any? %>
<div id="error_explanation">
	<h2><%= t 'errors.header.male', :resource => 'tipo de inmueble', :count => @type.errors.count %></h2>
	<ul>
		<% @type.errors.each do |attr, msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<fieldset class="gray">
	<div class="clearfix">
		<%= f.label :name %>
		<div class="input">
			<%= f.text_field :name %>
		</div>
	</div>
	<div class="clearfix">
		<%= f.label :icon %>
		<div class="input">
			<%= f.file_field :icon %>
		</div>
	</div>
	<div class="actions">
		<%= f.submit 'Guardar', :class => "btn primary" %>
	</div>
</fieldset>
<% end %> 